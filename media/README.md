# Evidências — Back-End (Parte 2)

Coloque nesta pasta os seguintes prints (exigidos pela atividade):

1. **rota-base-codespaces.png** — rota `/` funcionando no navegador via Codespaces,
   com o back-end executando em container Docker.
2. **rota-v1-codespaces.png** — rota `/v1` funcionando no navegador via Codespaces,
   com retorno de data/hora, em container Docker.
3. **render-events.png** — painel **Events** do Render com o histórico de deploy.
4. **tag-v1.1.0.png** — evidência da tag `v1.1.0` (ex.: aba Tags/Releases do GitHub).

> Dica: para o item 1 e 2, rode `docker compose up --build` no Codespaces,
> abra a porta 5000 encaminhada e tire o print do navegador mostrando o JSON.
